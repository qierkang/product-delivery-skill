#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
echo "[sync] root=${ROOT_DIR}"
echo "[sync] 按需将 product-delivery-skill 同步到目标技能目录（OpenClaw/Claude/Codex）"
echo "[sync] 当前脚本仅保留占位，后续按目标运行端补充实装"

