# 配置入口说明

默认先改：

- `profile.yaml`
- `tech-stack.md`
- `environments.md`（尤其数据库与缓存连接口径）

项目切换时至少更新：

- `workspace_dir`
- 数据库/缓存类型
- 支持的执行模式

## sbti-red 当前默认连接口径

- MySQL：`jdbc:mysql://127.0.0.1:3306`，`root / qierkang`
- Redis：`127.0.0.1:6379`，密码 `qierkang`
