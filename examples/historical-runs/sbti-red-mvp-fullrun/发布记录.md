# 发布记录

## 元信息

- 版本：`v1.0`
- 日期：`2026-04-12`
- 发布人：`xyqierkang@gmail（claude+codex）`
- 目标环境：`local / dev`
- 发布策略：`Docker Compose 全量重建 + API/Web/Manage 冒烟`
- 默认结论：`READY`

## 1. 发布前检查

| 检查项 | 结果 | 说明 |
|---|---|---|
| 开发放行通过 | 是 | `开发放行报告.md` 结论为 `READY` |
| 代码审查通过 | 是 | 无 P0/P1 阻塞 |
| 冒烟通过 | 是 | 冒烟脚本和冒烟报告均已完成 |
| QA/UI/产品验收通过 | 是 | 三份验收报告均已补齐 |
| 回滚方案明确 | 是 | 可 `docker compose down` 回滚并恢复旧镜像 |

## 2. 发布执行

| 步骤 | 结果 | 说明 |
|---|---|---|
| API 发布 | 通过 | `docker compose ... up -d --build api` |
| Web 发布 | 通过 | `docker compose ... up -d --build web` |
| APP/小程序/H5 发布 | 通过 | 当前 H5 发布在 `http://127.0.0.1:18080` |

## 3. 发布后验证

| 检查项 | 结果 | 说明 |
|---|---|---|
| 服务健康检查 | 通过 | `http://127.0.0.1:18081/health` 返回 `200` |
| 页面可访问 | 通过 | `web/manage` 均返回 `200` |
| 关键链路验证 | 通过 | 报告生成、海报生成、后台查看、邮件发送均已验证 |

## 4. 回滚信息

- 是否触发回滚：`否`
- 回滚原因：`无`
- 回滚版本：`如需回退，可恢复上一版镜像并执行 docker compose up -d`

## 5. 发布前 / 发布后 / 回滚补充说明

- 发布前：已确认数据库 `sbti_red` 和 17 张业务表初始化成功。
- 发布后：已写入真实测试数据，当前 `visitor_sessions=4`、`report_snapshots=4`、`page_events=49`、`email_jobs=2`。
- 回滚：如出现严重问题，可执行 `docker compose -f docker-compose.yml -f docker-compose.app.yml down`，恢复旧镜像后重新拉起。
