# 冒烟测试报告

## 元信息

- 版本：`v1.0`
- 日期：`2026-04-12`
- 执行人：`xyqierkang@gmail（claude+codex）`
- 执行环境：`local / dev`
- 执行脚本：`冒烟测试脚本.sh`
- 默认结论：`READY`

## 1. 冒烟范围

- 页面范围：H5 首页、报告页、运营后台首页
- API 范围：健康检查、session、题库、提交、报告、海报、后台登录与概览
- 数据范围：`visitor_sessions`、`report_snapshots`、`poster_assets`、`page_events`、`email_jobs`

## 2. API 健康检查

| 检查项 | 结果 | 说明 |
|---|---|---|
| 健康检查接口 | 通过 | `http://127.0.0.1:18081/health` 返回 `200`，`mysql/redis/storage=true` |
| 登录鉴权 | 通过 | `POST /api/manage/login` 成功获取 token |
| 核心业务 API | 通过 | `bootstrap/questions/submit/report/poster` 已全部打通 |

## 3. 页面主链路检查

| 检查项 | 结果 | 说明 |
|---|---|---|
| 页面可访问 | 通过 | `web:200`、`manage:200` |
| 列表/查询 | 通过 | 后台用户、报告、事件列表可查；报告深链可查 |
| 新增/编辑/查看 | 通过 | session 新增、昵称编辑、报告查看均正常 |
| 删除/提交 | 通过 | 提交测试正常；本轮无用户删除需求 |

## 4. 问题与回流

| 编号 | 问题 | 影响范围 | 回流对象 | 处理建议 |
|---|---|---|---|---|
| SM-01 | 163 IMAP 返回 `Unsafe Login`，无法自动做收件箱回查 | 邮件验证闭环 | 后端 / 运维 | 线上使用白名单 IP 或单独回执通道 |

## 5. 证据清单

| 证据ID | 类型 | 路径/日志 | 对应结论 |
|---|---|---|---|
| EVID-SMK-001 | 命令 | `bash 冒烟测试脚本.sh` | 核心主链路通过 |
| EVID-SMK-002 | 命令 | `curl web/api/manage` 状态码均为 `200` | 页面可访问 |
| EVID-SMK-003 | 数据 | `sbti_red.report_snapshots / poster_assets / page_events` | 数据落库成功 |
